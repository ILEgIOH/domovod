"""Заглушка идентификации пользователя MAX.

Когда приложение будет открываться как мини-апп внутри MAX, данные о
пользователе (стабильный id, имя) будут приходить автоматически через
MAX Bridge (`window.WebApp`) при каждом запуске — без пароля и форм.
Пока интеграция не подключена, эта заглушка выдаёт стабильный
фейковый профиль на устройство (браузер), чтобы можно было
продемонстрировать бесшовный сценарий входа по QR/ссылке.
"""

import secrets

STUB_DISPLAY_NAME = "Житель MAX"


def generate_device_id() -> str:
    """Имитирует id пользователя MAX, привязанный к устройству."""
    return f"max_stub_{secrets.token_hex(6)}"
